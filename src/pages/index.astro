---
import { DESIGN } from '../config';
import HomeGallery from '../components/Home.astro';
import HomeTerminal from '../components/HomeTerminal.astro';
import worksData from '../data/works.ja.json';
import type { Work } from '../lib/types';
const works = worksData as Work[];
---

{DESIGN === 'terminal' ? (
  <HomeTerminal lang="ja" works={works} />
) : (
  <HomeGallery lang="ja" works={works} />
)}
